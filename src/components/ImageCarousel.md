Infinitely Scrollable carousel of images

```jsx
import {ImageCarousel} from 'mvp-webapp'
import logo from '../images/external/logo.png';

const imgs = [logo, logo, logo, logo];

<ImageCarousel images={imgs} />

```